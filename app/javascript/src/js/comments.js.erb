$(document).ready(() => {
    setInterval(() => {
        $('#comments').html("<%= j render 'coment' %>");
        $.ajax({
            url: '/collections/' + params[:collection_id] + '/items/comments',
            success: (data) => {
              console.log(data)
            }
          });
    }, 2000);
});